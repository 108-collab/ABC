if (!require("BiocManager"))
    install.packages("BiocManager")
BiocManager::install("GenomicRanges") #to install the package
library(GenomicRanges) #to load into R session


#split mm9 file into files(extract rows)with same characters in the first column eg "chr1" 
same gene-id  file contains
same gene same transcript id rows together


